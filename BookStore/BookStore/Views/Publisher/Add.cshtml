@model BookStore.Models.Domain.Publisher
@{
    ViewData["Title"] = "Add";
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-10">
            <h3>Add Publisher</h3>
            <form asp-action="Add" method="post">
                <div class="form-group">
                    <label asp-for="PublisherName">Publisher Name *</label>
                    <input type="text" class="form-contorl" asp-for="PublisherName" />
                    <span asp-validation-for="PublisherName" class="text-danger"></span>
                </div>
                    @if (TempData["msg"]!= null)
                    {
                    <div class="form-group">
                        @TempData["msg"]
                    </div>
                }
                <div class="form-group pt-3">

                <button type="submit" class="btn btn-dark">Save</button>
                <a asp-for="Publisher" asp-action="GetAll" class="btn btn-primary">List</a>

                </div>
            </form>
        </div>
    </div>

</div>

